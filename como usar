C00LUI - Biblioteca GUI para Roblox

C00LUI é uma biblioteca minimalista e poderosa para criar GUIs customizadas no Roblox. Com ela você pode facilmente criar:

Janelas com título personalizado

Várias páginas (tabs) navegáveis

Sections (colunas) dentro das páginas

Botões com callbacks

Toggle Open/Close automático da GUI

📦 Como instalar / usar

Basta carregar a lib via loadstring:

local c00lgui = loadstring(game:HttpGet("https://raw.githubusercontent.com/cubodegelo1116/c00lui/refs/heads/main/c00lui.lua"))()


Depois, crie a janela principal:

local win = c00lgui.Window({
    Title = "Teste C00L",                  -- Nome da GUI
    AccentColor = Color3.fromRGB(0,255,128) -- Cor de destaque (verde claro)
})

🔹 Criando páginas (Tabs)

Para criar uma página/tab:

local page1 = win:AddPage()


Cada página criada é automaticamente adicionada à navegação (< e >)

A primeira página criada é a página inicial visível

🔹 Criando sections (colunas dentro da página)

Dentro de cada página, você pode criar sections:

local sec1 = page1:AddSection("Movimento")
local sec2 = page1:AddSection("Pulo/Fly")


Sections ficam lado a lado (primeira esquerda, segunda direita, terceira esquerda, etc.)

Cada section pode ter quantos botões você quiser

🔹 Adicionando botões

Dentro de cada section, você adiciona botões com:

sec1:AddButton("Aumentar Speed", function()
    local plr = game.Players.LocalPlayer
    if plr.Character and plr.Character:FindFirstChild("Humanoid") then
        plr.Character.Humanoid.WalkSpeed = 50
        print("Speed aumentado!")
    end
end)


Parâmetros:

Texto do botão

Função callback executada ao clicar

🔹 Navegação entre páginas

A lib cria automaticamente botões < e > na parte superior:

-- "<" vai para a página anterior
-- ">" vai para a próxima página


Você não precisa criar nada para isso.

🔹 Botão Open/Close da GUI

A lib cria um botão toggle para minimizar/abrir a GUI:

-- O botão troca o texto automaticamente entre "Open" e "Close"
-- e esconde ou mostra a janela
